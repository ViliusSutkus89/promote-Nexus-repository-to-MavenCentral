name: 'build'
on:
  pull_request:
  push:
    branches:
      - main
      - 'releases/*'
      - test

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: yarn install
